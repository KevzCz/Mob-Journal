Hovered text now appears where it is written instead of jumping to the start of the line

Added hover previews: hovering a word can show an item, a texture, or every item in a tag

Tag previews come in two styles - a single cycling icon, or a 4x4 grid that pages through larger tags

Textures now accept width, height, srcwidth and srcheight

Added {left}, {center} and {right} line markers; headings are now centered by default

Descriptions now wrap by word instead of breaking between phrases

Health and armor no longer print a trailing .0, and stat rows that resolve to N/A are hidden

Added built-in descriptions for wolves, cats, parrots, horses, cows, sheep, chickens, pigs, rabbits and llamas

1.21.1's tags differ from 1.20.1 so there are differences between the 2 versions' descriptions